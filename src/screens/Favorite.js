import {Text, SafeAreaView} from 'react-native'

export default function Favorite(){
  return (
    <SafeAreaView style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }}>
    <Text>Favorite</Text>
    </SafeAreaView>
  )
}
