import {Text, SafeAreaView} from 'react-native'

export default function Account(){
  return (
    <SafeAreaView  style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }} >
    <Text>Account</Text>
    </SafeAreaView>
  )
}
